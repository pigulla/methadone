INSERT INTO
    channel_filters (
        id,
        network_id,
        `key`,
        name,
        title,
        text,
        created_at,
        updated_at
    )
VALUES
    (?, ?, ?, ?, ?, ?, ?, ?);
